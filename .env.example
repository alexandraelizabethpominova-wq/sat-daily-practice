# Optional cloud sync. The app works locally without these.
# Use a Supabase publishable key for browser clients. Legacy anon keys remain supported as a fallback.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
